<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flashsale.mapper.SeckillOrderMapper">

    <resultMap id="seckillOrderMap" type="com.flashsale.entity.SeckillOrder">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="event_id" property="eventId"/>
        <result column="order_id" property="orderId"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_orders (user_id, product_id, event_id, order_id, status)
        VALUES (#{userId}, #{productId}, #{eventId}, #{orderId}, #{status})
    </insert>

    <select id="selectByUserIdAndEventId" resultMap="seckillOrderMap">
        SELECT * FROM seckill_orders
        WHERE user_id = #{userId} AND event_id = #{eventId}
    </select>

    <select id="selectByOrderId" resultMap="seckillOrderMap">
        SELECT * FROM seckill_orders WHERE order_id = #{orderId}
    </select>

    <update id="updateStatus">
        UPDATE seckill_orders SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
